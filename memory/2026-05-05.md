# Memory - 2026-05-05

## Trading Day Summary (May 4, 2026)

### NY KZ Results
- 8 signals, total P&L: **-$2,170**
- MGC.F LONG hit SL @ 4628.7 → -$2,001
- MCL.F SHORT hit SL → -$202
- SIL.F SHORT won → +$317
- All other trades lost

### Strategy Adjustment
- New params: seq_len=20, threshold=0.4
- Added EMA12/EMA26 filter
- Added FVG filter
- Backtest with new params: 9 trades (8 LONG, 1 SHORT)
- Best case TP1 hit: +$5,400

### Data Download Status
- Downloaded 2 years daily data for: MES, MNQ, M2K, M6E, M6A, SIL, MGC
- MCL, MBT, MET: Yahoo Finance has no history (1 bar only)
- Need to resample old 15min data for these 3 symbols

### Key Decisions
- MGC.F 2 contracts = too big, caused -$2k loss
- New strategy filters more aggressively (threshold 0.4)
- MET.F/MBT.F still have problematic SL calculation (POINT_VALUE=0.1 → 2000 pts)

## System Fixes
- Fixed `node` path issue in kz_scheduler_lstm.js (changed bare `node` to `/usr/local/bin/node`)
- Telegram bot working with new token `8606567428:AAFvcsiNf00mAIES6-CTIwKeQTKaos0trNY`

## Current Status
- Ready to run backtest with 7 symbols (502 bars each)
- MCL/MBT/MET still need 15min resample solution
## ONNX Attempt (MGC_lstm.onnx)

### Goal
Convert MGC.F LSTM model to ONNX format for use with a Node.js ONNX runtime.

### Status
- ✅ MGC_lstm.keras model created (62KB) - trained with seq_len=20, 5 epochs
- ✅ MGC_scaler.json saved with min/max/seq_len
- ❌ tf2onnx conversion FAILED

### Error
```
tf2onnx cannot convert Keras LSTM - internal while-loop control flow ops 
not compatible with ONNX opset 13
```

### Root Cause
TensorFlow 2.21 + tf2onnx 1.15.1 have dependency conflicts:
- tensorflow 2.21 requires protobuf<8.0.0,>=6.31.1
- tf2onnx needs protobuf 3.20.3

### Options
1. Keep Python Keras inference (works fine)
2. Try CNN instead of LSTM (can convert to ONNX)
3. Fix dependency hell (hard)

### Decision Pending (user hasn't responded)

---

## Journal P&L Win Rate Fix (May 5, 2026)

### Problem
Journal missing P&L and win rate calculations.

### Fix Applied
Updated auto_update_journal.py with:
- P&L calculation per trade based on direction, entry, SL, TP1, close
- Daily win/loss count
- Win rate calculation
- Cumulative P&L tracking
- journal_daily_summary.csv for daily summaries

### New Cron (15:06 UTC daily)
```
06 09,15 * * * /usr/bin/python3 /home/node/.openclaw/workspace/auto_update_journal.py >> /tmp/auto_journal.log 2>&1
```

### New Files
- /home/node/.openclaw/workspace/auto_update_journal.py (updated)
- /home/node/.openclaw/workspace/journal_daily_summary.csv (created)